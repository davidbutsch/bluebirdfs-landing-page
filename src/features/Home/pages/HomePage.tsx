import { Page } from "@/components";
import { Typography } from "@mui/material";

export const HomePage = () => {
  return (
    <Page mt={5}>
      <Typography variant="h2" align="center" fontWeight={500}>
        Header content Header content Header content Header content
      </Typography>
    </Page>
  );
};
